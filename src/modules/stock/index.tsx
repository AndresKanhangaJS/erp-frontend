import { PageHeader } from '@/shared/components/layout/PageHeader'

/** Placeholder — as páginas reais (artigos, movimentos, inventário, ...) chegam no Passo 12. */
export default function StockModule() {
  return <PageHeader title="Stock" />
}
