import { PageHeader } from '@/shared/components/layout/PageHeader'

/** Placeholder — as páginas reais (funcionários, vencimentos, ...) chegam no Passo 11. */
export default function RhModule() {
  return <PageHeader title="RH" />
}
