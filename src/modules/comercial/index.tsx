import { PageHeader } from '@/shared/components/layout/PageHeader'

/** Placeholder — as páginas reais (clientes, pipeline, propostas, ...) chegam no Passo 12. */
export default function ComercialModule() {
  return <PageHeader title="Comercial" />
}
