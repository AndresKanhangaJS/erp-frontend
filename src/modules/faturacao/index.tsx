import { PageHeader } from '@/shared/components/layout/PageHeader'

/** Placeholder — as páginas reais (documentos, emitir, clientes, ...) chegam no Passo 9. */
export default function FaturacaoModule() {
  return <PageHeader title="Facturação" />
}
